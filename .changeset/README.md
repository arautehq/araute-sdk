# Changesets

Add a changeset to pull requests that change the published SDK:

```bash
bun run changeset
```

Choose the appropriate version bump and describe the user-facing change. The release workflow combines pending changesets into a version and changelog pull request.
